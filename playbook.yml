---
- hosts: 127.0.0.1
  connection: local
  tasks:
    - name: Update & Upgrade APT packages
      become: true
      apt:
        update_cache: true
        upgrade: yes

    - name: Install new packages
      become: true
      apt:
        name:
          - nano
          - jq
          - curl
          - git
        state: latest